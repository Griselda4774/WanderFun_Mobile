package com.example.wanderfunmobile.presentation.ui.custom.listeners;

import com.example.wanderfunmobile.domain.model.checkins.CheckIn;

public interface OnCheckInSelectedListener {
    void onCheckInSelected(CheckIn checkIn);
}
