<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_logo_rounded"
        android:supportsRtl="true"
        android:theme="@style/Theme.WanderFunMobile"
        tools:targetApi="31">
        <activity
            android:name=".infrastructure.ui.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".infrastructure.ui.activities.LoginActivity"
            android:exported="false"
            android:theme="@style/ActivityTheme" />
        <activity
            android:name=".infrastructure.ui.activities.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".infrastructure.ui.activities.MainActivity"
            android:exported="false"
            android:theme="@style/ActivityTheme" />
    </application>

</manifest>